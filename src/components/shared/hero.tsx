import Image from 'next/image'
import { Section } from '../ui/section';

function Hero() {
  return (
    <Section variant={"fullWidth"} className="h-100 bg-slate-100">
      <Image src={''} alt={''} className='' />
    </Section>
  );
};

export { Hero };
